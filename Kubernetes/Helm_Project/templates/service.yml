---
apiVersion: v1
kind: Service
metadata: 
    name: hpcc-service
    namespace: {{ .Values.namespace }}

spec:
    selector:
        app: hpcc
    
    type: NodePort

    ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      NodePort: 30090